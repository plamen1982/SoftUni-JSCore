function solve([key1, value1, key2, value2, key3, value3]) {
    let keyValueObject = {};
    keyValueObject[key1] = value1;
    keyValueObject[key2] = value2;
    keyValueObject[key3] = value3;
    console.log(keyValueObject)
}

// solve(['name', 'Pesho', 'age', '23', 'gender', 'male']);
solve(['ssid', '90127461', 'status', 'admin', 'expires', '600']);